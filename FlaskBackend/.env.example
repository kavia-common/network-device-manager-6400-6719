# Example environment configuration for the Flask Backend.
# You may copy this file to ".env" and adjust values as needed.
# If any of these are omitted, the application will fall back to sensible defaults:
# - MONGO_URI defaults to "mongodb://localhost:27017"
# - DB_NAME defaults to "network_device_manager"
# - COLLECTION_NAME defaults to "devices"

# MongoDB connection string (override default if needed)
MONGO_URI=mongodb://localhost:27017

# Database name (override default if needed)
DB_NAME=network_device_manager

# Collection name (override default if needed)
COLLECTION_NAME=devices
